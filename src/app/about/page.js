import Hero from "@/components/Hero";

export default function About() {
  return (
    <div>
      {/* TODO: pass the about hero photo via the `image` prop */}
      <Hero eyebrow="OUR HISTORY" title="ABOUT US" />
      {/* page content */}
    </div>
  );
}
