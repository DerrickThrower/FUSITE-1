import Hero from "@/components/Hero";

export default function Programs() {
  return (
    <div>
      {/* TODO: pass the programs hero photo + eyebrow via props */}
      <Hero title="PROGRAMS" />
      {/* page content */}
    </div>
  );
}
